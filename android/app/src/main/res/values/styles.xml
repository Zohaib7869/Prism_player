<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Parented on MaterialComponents (an AppCompat descendant) because
         androidx BiometricPrompt inflates AppCompat-themed views; a plain
         android:Theme.Black parent makes the biometric sheet fail to show. -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>
    <style name="NormalTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>
</resources>
